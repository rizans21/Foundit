{
  "name": "lost-and-found",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/dom": "^10.4.0",
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/react": "^16.2.0",
    "@testing-library/user-event": "^13.5.0",
    "body-parser": "^2.2.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "js-cookie": "^3.0.5",
    "mongoose": "^8.13.1",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-router-dom": "^7.4.1",
    "react-scripts": "5.0.1",
    "web-vitals": "^2.1.4",
    "nodemon": "^2.0.20"  // Nodemon for automatic restart during development
  },
  "scripts": {
    "start": "node src/index.js",  // Start backend with Node.js
    "dev": "nodemon src/index.js", // Start backend with Nodemon for development (auto-restarts on changes)
    "build": "react-scripts build", // Build React frontend for production
    "start:frontend": "react-scripts start", // Start React frontend development server
    "test": "react-scripts test",  // Test React frontend
    "eject": "react-scripts eject" // Eject React configuration if needed
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
